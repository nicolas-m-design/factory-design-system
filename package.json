{
  "name": "token-brand-system",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:tokens": "node scripts/generate-tokens.mjs",
    "dev": "npm run generate:tokens && vite",
    "build": "npm run generate:tokens && tsc -b && vite build",
    "preview": "vite preview",
    "test": "npm run generate:tokens && vitest run",
    "test:watch": "npm run generate:tokens && vitest"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.14.0",
    "remixicon": "^4.9.1"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/jest-axe": "^3.5.9",
    "@types/node": "^25.5.2",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "jest-axe": "^10.0.0",
    "jsdom": "^29.0.2",
    "typescript": "~5.7.0",
    "vite": "^6.4.2",
    "vitest": "^4.1.3"
  }
}
